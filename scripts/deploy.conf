#!/bin/bash
# 部署配置
# 请根据实际情况修改以下变量

# 服务器信息
SERVER_HOST=your-server-ip
SERVER_USER=your-server-user
SERVER_PORT=22

# 服务器部署目录
DEPLOY_DIR=/opt/max-demands

# 应用配置
JAR_NAME=max-demands.jar
PROFILE=prod
SERVER_PORT=8183

# 本地构建产物路径
LOCAL_JAR=target/max-demands-*.jar
